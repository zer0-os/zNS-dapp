export enum Errors {
	// ----- Generic ----- //
	FAILED_TRANSACTION = 'Failed to submit transaction',
	FAILED_METADATA_UPLOAD = 'Failed to upload metadata.',
	REJECTED_WALLET = 'Transaction denied by wallet',
}

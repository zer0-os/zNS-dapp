import { StakePoolTable } from 'containers/staking';

const StakePools = () => {
	// This file looks empty because we used to
	// have some StatWidets in here - if we need to add
	// them back, put them in a <ul> here

	return <StakePoolTable />;
};

export default StakePools;
